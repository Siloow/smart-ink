VITE_RENDER_URL=https://smart-ink-render-615593848551.europe-west4.run.app

# Where landing-page beta requests are delivered (Formspree, Tally, Basin, …).
# While this is empty every request is kept only in the visitor's own browser
# and never reaches you. Set it before inviting anyone. See .env.example.
VITE_WAITLIST_ENDPOINT=
